// Remote executable advertisements must never run in the local control UI.
// Static, explicitly opened sponsor links remain in SponsorBar / Settings.
export default function AdSlot(){ return null; }
